var nums = [100,25,20,72];
var words = ['Animals','mONKEYS',"chairs"];

var bubbles[];
/*var bubble = {
    x: 300,
    y: 200,
    display: function(){
        stroke(255);
        noFill();
        ellipse(mouseX, mouseY,24,24);
        /*ellipse(this.x,this.y,24,24); 
    },
    update: function(){
        this.x = this.x + random(-1,1);
        this.y = this.y + random(-1,1);
    }
}*/

function setup(){
    createCanvas(600,400);
    /*var bubble = {
        x: 300,
        y: 200,
        display: function(){
            stroke(255);
            noFill();
            ellipse(mouseX, mouseY,24,24);
            /*ellipse(this.x,this.y,24,24); 
        },
        update: function(){
            this.x = this.x + random(-1,1);
            this.y = this.y + random(-1,1);
        }
    }*/
}
function draw(){
    background(0);
    fill (255);
    /*for (var i=0; i<10; i++){
    bubble.display();
    bubble.update();*/
    }
    /*for (var i=0; i<nums.length; i++){
        stroke(255);
        noFill();
        ellipse(i*100+100,i*100+100,nums[i],nums[i]);
        ;
    }
    for (var i=0; i<nums.length; i++){
        textSize(20);
        text(words[i],i*100+100,200)
    }*/
    
}